#!/usr/bin/env node
// Placeholder: the original outreach CLI is not present in this checkout (the
// file had been overwritten by a stray git error message). Outreach features
// are available through the working entry points below.
//
// Usage:
//   • Cover letter:   node generate-cover-letter.mjs --payload <json>
//   • LinkedIn/email: web dashboard Outreach tab (/api/outreach/*)

console.error(
  "outreach.mjs is a placeholder in this checkout.\n" +
    "Use `node generate-cover-letter.mjs` or the web dashboard's Outreach tab instead."
);
process.exit(1);
